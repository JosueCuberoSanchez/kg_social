/**
 * Enrolled events page.
 * @author Josué David Cubero Sánchez.
 */

import React from 'react';

import EnrolledEventsContainer from "../../containers/enrolled-events/";

let EnrolledEvents = ()  => {

    return (
        <EnrolledEventsContainer isLoading={true}/>
    );
};

export default EnrolledEventsContainer;